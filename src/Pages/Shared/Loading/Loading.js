import React from 'react';

const Loading = () => {
    return (
        <div className='min-h-screen flex justify-center absolute'>
            <div class="flex items-center justify-center ">
                <button className="btn loading">loading</button>
            </div>
        </div>
    );
};

export default Loading;